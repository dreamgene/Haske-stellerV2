[package]
name = "payment_core"
version = "0.1.0"
edition = "2021"
description = "HASKEpay payment provider abstractions for Lightning-native settlement."
license = "MIT"

[dependencies]
anyhow = { workspace = true }
async-trait = { workspace = true }
shared_types = { path = "../shared_types" }
