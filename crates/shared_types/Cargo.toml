[package]
name = "shared_types"
version = "0.1.0"
edition = "2021"
description = "Shared HASKEpay API types for Lightning payment requests and access status."
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
